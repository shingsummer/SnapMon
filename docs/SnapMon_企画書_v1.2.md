# SnapMon（仮）ゲームデザイン仕様書

写真で生まれ、歩いて育つ、育成バトルRPG

- バージョン: 1.2（2026-09-11）
- v1.1 変更点: 素質・成長タイプは**永久に非開示**（鑑定書廃止）、**寿命を廃止**し「師匠システム」に置換、素質による上限を緩和（どの個体も実用圏に届く）
- v1.2 変更点: つぶやきシステム（§4.6、解析されない確率型の成長タイプ示唆）、師匠の型（§5.5、技の継承・5枠目）を追加
- 対象: 本番リリース想定の完全版仕様
- 用途: この文書は Claude Code（VSCode）に渡して実装を進めるための一次仕様書である。実装者（Claude Code）は本書の「第15章 実装の進め方」から読み始め、疑問点は本書内の該当章を参照すること。

---

## 目次

1. コンセプト
2. コアループ
3. モンスター生成システム（写真→モンスター）
4. ステータスと成長タイプ
5. 育成システム（歩数・トレーニング・アイテム）
6. バトルシステム
7. 制限と不正対策
8. 画面一覧とUIフロー
9. 技術構成
10. データモデル
11. サーバーAPI設計
12. マネタイズ
13. 開発フェーズとマイルストーン
14. コスト見積もりとリスク・法務
15. 実装の進め方（Claude Codeへの指示）
16. 付録（乱数式・定数表・プロンプトテンプレート）

---

## 1. コンセプト

### 1.1 一言で

身の回りの「物」を写真に撮るとモンスターになる。そのモンスターは、外に出て歩くことでしか強くならない。

### 1.2 元ネタとの差別化

| 要素 | 元ネタ | 本作での扱い |
|---|---|---|
| モンスターの誕生 | モンスターファーム（CD/音楽から生成） | 写真から生成。被写体の「何か」「色」「形」が種族・属性・見た目に反映される |
| 成長のランダム性 | モンスターファーム（個体差・寿命） | 成長タイプ（早熟／晩成／平均／波型）と素質値をランダム付与し、どちらも非開示。「最初弱いが化ける」個体が存在。寿命は採用しない |
| 位置情報・歩行 | ポケモンGO | 歩数と歩行距離が育成の主エネルギー源。乗り物移動はカウントしない |
| 収集制限 | 課金ガチャ全般 | 1日の生成枚数を固定上限にし、課金でも撮影枚数は増やせない |

### 1.3 設計上の三原則

1. **強さは歩いた量でしか買えない**: 課金・周回・連写で強くなる抜け道を作らない。
2. **育ててみないと分からない**: 素質と成長タイプは数値として一切開示しない。プレイヤーが知る手段は「実際に育てて成長グラフを見る」ことだけ。「早熟だから捨て、晩成だから育てる」という作業化を防ぐ。
3. **モンスターは死なない・捨てさせない**: 寿命なし。成長が止まった個体は「師匠」として次世代を支え、牧場に居続ける。
4. **写真は思い出であり材料**: 撮った写真はモンスターの「出自」として図鑑に残る（本人の端末内のみ）。

### 1.4 ターゲット

- メイン: 20〜40代、ポケモンGO／モンスターファーム経験者、通勤・散歩習慣のある層
- サブ: 親子で遊ぶ層（子どもが撮る、親が歩く）

### 1.5 プラットフォーム

- iOS 16 以上 / Android 10 以上
- 縦画面固定、オフライン時は閲覧と歩数蓄積のみ可能、生成・バトルはオンライン必須

---

## 2. コアループ

```
[撮る] 1日3枚まで
   ↓ 画像認識 → シード生成 → ステータス確定 → AI画像生成
[生まれる] 初期ステータスのみ表示。成長タイプ・素質は永久に隠し値
   ↓
[歩く] 歩数 → 「活力ポイント（VP）」に変換（乗り物は除外）
   ↓
[育てる] VPを消費してトレーニング／アイテム使用 → ステータス上昇（成長タイプで伸び方が変わる）
   ↓
[戦う] 近所のジム（POI）・すれ違い・フレンド対戦 → 報酬アイテム／VP
   ↓
[師匠・継承] Lv50 到達で「師匠」になれる → 新しく生まれる1体に素質の一部を継承（師匠は牧場に残り、バトルも可能）
   ↺
```

1セッションの想定時間: 撮影1分、育成操作2〜3分、バトル1〜2分。1日合計10分＋歩行時間。

---

## 3. モンスター生成システム（写真→モンスター）

### 3.1 生成パイプライン

```
写真（端末）
 → ① 端末内前処理（縮小 1024px、EXIF除去、顔検出→顔があれば撮り直し要求）
 → ② サーバーへ送信（HTTPS、認証必須）
 → ③ 画像認識API: ラベル(上位5)・主要色(上位3)・アスペクト比・輝度 を取得
 → ④ シード計算: hash(ラベル1, 主要色1, userId, 日付, サーバー乱数)
 → ⑤ 種族決定 / 属性決定 / ステータス・成長タイプ・素質を乱数決定（§4）
 → ⑥ 画像生成API に「種族＋属性＋色＋被写体特徴」プロンプトを送信（§16.3）
 → ⑦ 生成画像をストレージに保存、Monsterレコード作成、クライアントに返却
```

- ③〜⑦はすべてサーバー側で行う。クライアントはステータス乱数を一切生成しない（改ざん防止）。
- ⑥の画像生成は非同期。生成完了まではシルエット（卵）表示。通常30〜60秒。
- 生成失敗時はリトライ2回、失敗ならその日の撮影枠を返却してユーザーに再撮影を促す。

### 3.2 種族（ファミリー）決定ルール

画像認識ラベルを 12ファミリーにマッピングする。ラベル辞書はサーバー側JSONで管理し、無料で更新できるようにする。

| ファミリー | 対応ラベル例 | 基本傾向 |
|---|---|---|
| ビースト | 犬, 猫, ぬいぐるみ, 毛皮 | 攻撃・速さ寄り |
| プラント | 花, 木, 野菜, 観葉植物 | HP・回復寄り |
| メタル | 工具, 車, 鍵, 缶 | 防御寄り |
| アクア | ペットボトル, コップ, 水, 魚 | バランス |
| ロック | 石, 建物, コンクリート | 防御・HP |
| スパーク | 電化製品, ケーブル, 照明 | 速さ・特攻 |
| ゴースト | 古い物, 人形, 暗い写真 | 特攻・回避 |
| フード | 食べ物, 菓子, 果物 | HP・運 |
| ペーパー | 本, 書類, 段ボール | 賢さ・回避 |
| クロス | 服, 靴, 布 | 速さ・バランス |
| トイ | おもちゃ, ゲーム機, フィギュア | 運・波型成長多め |
| エニグマ | 認識信頼度が低い／該当なし | 全ランダム、大当たり率も高い |

複合被写体（例: 猫のぬいぐるみ）は、ラベル上位2つが別ファミリーなら「サブファミリー」を持ち、見た目と技に両方が反映される。

### 3.3 属性（エレメント）決定ルール

主要色1位から決定。火（赤・橙）、水（青）、草（緑）、雷（黄）、光（白・淡色）、闇（黒・濃色）、無（灰・茶・複合）。相性は3すくみ＋光闇相互有利。

### 3.4 同じ物を撮ったときの挙動

シードに `userId` と `日付` を含めるため、同じマグカップを撮っても毎日違う個体になる。ただし種族と属性は同じになるため「この系統を狙う」遊びは成立する。

### 3.5 撮影バリデーション

- 人物の顔が検出された場合: 生成拒否、撮影枠は消費しない（プライバシー保護）
- スクリーン（ディスプレイ）撮影の検出: 認識ラベルに「screen/monitor」が上位にある場合は生成可だが「エニグマ」に強制。画面越しの画像検索連打を無意味化する
- 極端な暗さ／ブレ: 認識信頼度が0.3未満ならエニグマ

---

## 4. ステータスと成長タイプ

### 4.1 ステータス6種

| 略称 | 名称 | 役割 |
|---|---|---|
| HP | 体力 | 0で戦闘不能 |
| ATK | 攻撃 | 物理技の威力 |
| DEF | 防御 | 物理被ダメ軽減 |
| SPA | 特攻 | 属性技の威力 |
| SPD | 速さ | 行動順・回避 |
| LUK | 運 | 会心率・アイテムドロップ率 |

各ステータスの上限は 999。合計値（BST）で総合ランクを表示する。

### 4.2 個体が持つ隠しパラメータ

| パラメータ | 範囲 | 説明 | プレイヤーへの見え方 |
|---|---|---|---|
| 初期値 `base[s]` | 5〜60 | 生まれた時のステータス | 即時表示 |
| 素質 `talent[s]` | 1〜10 | 最終到達点を決める。10なら999まで届く | **永久に非表示**。数値・ランク・星などいかなる形でも出さない |
| 成長タイプ `growth` | 早熟／平均／晩成／波型／超晩成 | 伸び方の曲線 | **永久に非表示**。成長グラフ（実績値の推移）だけが手がかり |
| 性格 | 8種 | 好むトレーニングで効率±20% | 3回トレーニングで表示（育成の手触りに関わるので開示） |

**非開示の徹底ルール（実装者向け）**
- API レスポンスに `talent` `growth` を含めない。クライアントは存在自体を知らない。
- 成長グラフはレベルごとの実測ステータスをそのまま描く。予測線・上限線は描かない。
- 「伸びしろ」「期待値」を推定して表示する機能は作らない。ソート項目にも入れない。
- 例外: 図鑑の「生涯記録」にも載せない（Lv50 到達後も非開示。プレイヤーが「あの子は晩成だったんだな」と思うのは自由）。
- フレーバーテキストによる「示唆」は採用する。ただし §4.6 の「解析されない」ルールに従うこと。

### 4.3 「最初強いが伸びない／最初弱いが化ける」の実装

初期値と素質を **負の相関** で振る。

```
raw_base   = rand(5, 60)                  # 各ステータス独立
raw_talent = rand(1, 10)
# 相関係数 -0.5 程度になるよう調整
talent[s]  = clamp( round( raw_talent * 0.6 + (65 - raw_base) / 60 * 10 * 0.4 ), 1, 10 )
```

さらに成長タイプの抽選確率を初期値合計（BST0）で変える。

| BST0 | 早熟 | 平均 | 晩成 | 波型 | 超晩成 |
|---|---|---|---|---|---|
| 250以上（強い） | 50% | 35% | 10% | 5% | 0% |
| 150〜249 | 20% | 45% | 20% | 10% | 5% |
| 149以下（弱い） | 5% | 25% | 35% | 15% | **20%** |

超晩成は Lv30 までほぼ伸びないが、以降は素質上限まで一気に伸びる。「弱いのに手放さなかった人だけが強い個体を得る」を実現する。

### 4.4 レベルと成長曲線

- レベル上限: 50
- 経験値はトレーニングとバトルで獲得。
- レベルアップ時のステータス上昇量:

```
gain[s] = talent[s] * curve(growth, level) * personalityMod(s) * rand(0.8, 1.2)

curve(早熟,  L) = 2.0 - L/50 * 1.5   # 序盤2.0 → 終盤0.5
curve(平均,  L) = 1.25
curve(晩成,  L) = 0.5 + L/50 * 1.5   # 序盤0.5 → 終盤2.0
curve(波型,  L) = 1.25 + 0.75 * sin(L / 4)
curve(超晩成,L) = L < 30 ? 0.3 : 2.56
```

全成長タイプで Lv1→50 の積分がほぼ等しくなるよう定数を調整済み（各タイプの合計 ≒ 62.5 ± 5）。

### 4.5 素質によるステータス上限と最終到達点の目安

各ステータスの個体上限は `statCap[s] = 399 + talent[s] * 60`（素質1 = 459、素質5 = 699、素質10 = 999）。レベルアップ・トレーニング・強化石のどれで上げてもこの上限を超えない。

v1.0 の `talent*100-1` から緩和した理由: 素質が非開示なので、素質1の個体が 99 で頭打ちになると「知らずに育てた時間」が無駄に感じられる。最低でも 459 に届けば実用圏（ジム防衛・低ランクリーグ）で使えるため、どの個体を育てても損にならない。素質は「最強を目指すときだけ効く差」に留める。

- 素質10 / 初期60 / 早熟: Lv40 前後で 999 到達、その後は伸びない（「早く完成する」個体）
- 素質10 / 初期10 / 超晩成: Lv30 まで 20 前後、Lv50 で 999 到達（「化ける」個体）
- 素質3 / 初期40 / 平均: Lv35 前後で 579 に到達。十分戦える

上限到達を明示するUI（光る・「MAX」表示など）は **作らない**。上限値から素質が逆算できてしまうため。プレイヤーは成長グラフが平らになったことで気づく。ただしトレーニング画面で上限到達済みステータスを選んだ場合は VP を消費せず「もう伸びないようだ」とだけ返す（VP の無駄打ち防止）。

### 4.6 つぶやきシステム（成長タイプの示唆）

モンスターは時々ひとこと「つぶやく」（詳細画面・ホームのパートナー表示・トレーニング後）。このつぶやきの中に、成長タイプをうっすら示唆するものを混ぜる。目的は「Lv20で超晩成を諦めて保管送りにする」を減らすこと。

**設計の大前提: 「Lv○○になったら出る」「○日放置したら出る」のような、プレイヤーが条件を特定できる仕組みにしない。** そのために以下を守る。

1. **トリガーはレベルでも日数でもなく確率**。レベルアップ・トレーニング完了・ログイン時にそれぞれ独立に抽選する。示唆つぶやきの出現率は成長タイプごとの「示唆ウィンドウ」内で 1回あたり 8%、ウィンドウ外では 1%（ゼロにしない）。
   - 超晩成のウィンドウ: Lv12〜28（個体ごとに開始Lvを ±4 ランダムにずらす）
   - 晩成: Lv10〜25、早熟: Lv5〜15、波型: 常時 3%、平均: なし（ただし 1% の誤爆はある）
2. **同じテキストを複数の成長タイプで使う**。例えば「最近目つきが変わった気がする」は超晩成 60%・晩成 25%・波型 10%・早熟 5% の重みで出る。どのテキストも単独では確定情報にならない。
3. **示唆つぶやきと無関係なつぶやきを同じ見た目で出す**。「今日の散歩は楽しかった」「あの写真の場所、また行きたい」など無関係なものが全体の 85% 以上を占め、示唆つぶやきだけ目立つUIにしない。
4. **サーバー側で抽選し、抽選ログは保存しない**。テキストのIDだけをクライアントに返す。データマイニングでテーブルが割れても、重み付き確率なので「このセリフが出たら確定」にはならない。
5. **テキストプールは運営側で追加・入替可能**（shared-config の `murmurs.json`）。攻略サイトで対応表が出回っても、重みと文言を入れ替えれば無効化できる。
6. 「まったく出ない個体」も存在させる（個体ごとに `murmurRate` を 0.5〜1.5 倍でランダム化）。無口な子は無口。

つぶやき例（示唆系）: 「最近目つきが変わった気がする」「まだ本気を出していないような顔をしている」「ずいぶん落ち着いてきた」「今が一番元気なのかもしれない」「なにかを溜め込んでいるように見える」
つぶやき例（無関係）: 「散歩のあとは機嫌がいい」「雨の日はじっとしている」「出自の写真をじっと見ている」

期待される体験: 「なんか意味深なこと言ってたから、もうちょっと育ててみるか」。確定情報ではなく、迷いを後押しする程度。

---

## 5. 育成システム

### 5.1 活力ポイント（VP）: 歩行の通貨

- 端末の歩数計から **歩数** を取得し、サーバーで VP に変換する。
- 変換レート: `100歩 = 1 VP`。1日の上限は `12,000歩 = 120 VP`（歩きすぎ防止・健康配慮）。
- 追加ボーナス: 30分以上の連続歩行を1日1回達成で +10 VP。

### 5.2 乗り物除外ロジック

| 判定材料 | 実装 |
|---|---|
| OSのアクティビティ認識 | iOS: `CMMotionActivity`（walking / running / automotive / cycling）、Android: `ActivityRecognition API` |
| 速度 | GPSから算出した移動速度が **12 km/h 超** の区間は歩数を無効化 |
| 歩数の物理妥当性 | 1分あたり 220歩 超は無効（振り機対策） |
| 同期 | 端末は 5分ごとに「歩数・アクティビティ種別・平均速度」の区間データをサーバーに送信。サーバーが最終判定 |

走る（running）は歩行扱いで有効。自転車（cycling）・自動車（automotive）は無効。

### 5.3 トレーニング

VPを消費してステータスを上げる。1回 10 VP、1日最大8回（=80 VP）。残りのVPはアイテム交換や翌日繰越（上限200 VP）。

| トレーニング | 主上昇 | 副上昇 | 疲労 |
|---|---|---|---|
| ダッシュ | SPD | ATK | 中 |
| 力仕事 | ATK | HP | 高 |
| 瞑想 | SPA | LUK | 低 |
| 耐久 | DEF | HP | 高 |
| 散歩（自動） | 全て微増 | — | なし |

- 疲労が100を超えると翌日までトレーニング不可。休息（VP消費なし）で回復。
- トレーニングによる上昇はレベルアップ上昇とは別の「加算値」で、`+ rand(1, 3) * talent[s] / 5`。

### 5.4 アイテム

写真からモンスターを生成した際、被写体のファミリーに応じたアイテムが **1〜3個** 同時に出る。バトル報酬・ジム報酬でも入手。

| アイテム分類 | 効果例 |
|---|---|
| 餌（ファミリー別） | 対応ファミリーのモンスターに与えると経験値＋、性格一致で疲労回復 |
| 強化石 | 特定ステータス +5〜+15（素質上限は超えない。上限到達済みなら消費されない） |
| 絆カプセル | 師匠になるときの継承率を上げる |
| 疲労回復薬 | 疲労を 0 にする（1日1個まで使用可） |

鑑定書・延命薬は v1.1 で廃止（§4.2 非開示ルール、§5.5 寿命廃止による）。

### 5.5 師匠と継承（寿命の代替）

寿命は存在しない。モンスターは死なず、引退もしない。成長の終わりは Lv50 到達（または全ステータス上限到達）で、それ以降もバトル・ジム防衛・図鑑・着せ替えは通常どおり使える。

- Lv50 に到達した個体は「師匠」に任命できる（任意・取消不可）。
- 師匠は **1体につき1回だけ** 「弟子」を指名できる。弟子は「次に生まれるモンスター」で、生成時に師匠の素質の一部（各ステータス素質の 30%、絆カプセル使用で 50%）が上乗せされる（上限10）。
- 師匠は牧場に残り、ステータスも技もそのまま。弟子の詳細画面に「師匠: ○○」が表示され、師匠側にも「弟子: △△」が表示される。
- 弟子がさらに Lv50 になれば孫弟子を指名でき、系譜が図鑑に「家系図」として残る。累積で理想個体に近づくのが長期目標。
- 継承の中身（素質）は非開示のまま。プレイヤーには「師匠の力が受け継がれた」演出のみ。

**師匠の型（技の継承）**

- 師匠を任命するとき、師匠が覚えている 4 技のうち **1つ** を「師匠の型」として選ぶ（選んだ後は変更不可）。
- 弟子は誕生時にその技を **5枠目の専用スロット** に持って生まれる。通常の 4 枠は §6.3 のとおり自分のファミリー・属性から習得する。
- 師匠の型は弟子のファミリー・属性と無関係に使える（アクア族の弟子がビースト族の技を持てる）。これが家系ごとの個性になる。
- 弟子が師匠になるとき、師匠の型をさらに次へ渡せる（自分の 4 技から選んでも、受け継いだ師匠の型をそのまま渡してもよい）。3 代続けて同じ技が受け継がれると技名に「〜・伝承」が付き、威力 +10%（見た目と物語のためのボーナス、上限はここまで）。
- 家系図（S14）には各世代の師匠の型が表示される。

技継承はポケモンの「タマゴ技」に近い仕組みだが、本作では (a) 1 技のみ、(b) 専用スロットで通常枠を圧迫しない、(c) 系譜で見えることが目的、の 3 点で差別化する。強さより「うちの家系はずっとこの技」という愛着を狙う。

これにより「育てきった個体を手放す悲しさ」をなくし、逆に「最初に育てた1体がずっと家系の始祖として残る」体験にする。

---

## 6. バトルシステム

### 6.1 基本ルール

- 1対1、ターン制、最大 10 ターン、3体パーティから1体ずつ出す（勝ち抜き）。
- 行動は「技（4つまで）」から選択。オートバトルあり（非同期対戦向け）。
- 行動順: SPD 順。同値はランダム。

### 6.2 ダメージ式

```
damage = ( power * (ATK or SPA) / (DEF or SPA_DEF) ) * 0.4
         * typeMod(1.5 / 1.0 / 0.67)
         * crit(LUK/1000 の確率で 1.5倍)
         * rand(0.9, 1.1)
```

Lv50・平均ステータス（500）同士で、1発 15〜20% の HP を削る調整。

### 6.3 技

- ファミリーで 6 技、属性で 4 技のプールがあり、生成時に 2 技、レベルアップ（Lv10/20/30/40）で 1 技ずつ習得候補。
- サブファミリーがある個体は両方のプールから習得可能。
- 弟子は上記 4 枠に加えて「師匠の型」1 枠を持つ（§5.5）。バトルでは 5 技から選択。

### 6.4 対戦モード

| モード | 内容 | 報酬 |
|---|---|---|
| ジム | 実在のPOI（公園・駅など、OpenStreetMap ベース）に紐づく。半径 80m 以内で挑戦。防衛モンスターを置ける | VP、アイテム、ジムコイン |
| すれ違い | Bluetooth/位置で近くにいたプレイヤーと自動非同期対戦 | 少量 VP |
| フレンド | コード交換して非同期対戦 | 名誉のみ |
| 週間リーグ | レート制。同ランク帯と非同期マッチ | ランク報酬（見た目アイテム） |

### 6.5 ジムの占有バランス

- 1ジムに置けるのは 1人1体、最大 6体。
- 防衛モンスターは 24時間で自動帰還（張り付き防止）。
- 同一ジムへの挑戦は 1日 3回まで。

---

## 7. 制限と不正対策

### 7.1 1日の制限（コアバランス）

| 項目 | 上限 | 備考 |
|---|---|---|
| 写真→モンスター生成 | **3枚／日** | 課金でも増えない。日付はサーバーの JST 0:00 でリセット |
| 育成中モンスター（アクティブ枠） | 30体（無料）／100体（プレミアム） | 超えたら「保管牧場」へ移動（解放は任意。デフォルトで解放ボタンを目立たせない） |
| 保管牧場 | 500体 | 保管中はトレーニング不可、バトル参加不可。いつでもアクティブに戻せる |
| VP獲得 | 120 VP／日（12,000歩） | |
| トレーニング | 8回／日 | |
| ジム挑戦 | 同一ジム 3回／日 | |

### 7.2 不正対策

| 不正 | 対策 |
|---|---|
| 位置偽装 | サーバーで移動速度の連続性を検証（瞬間移動検知）。iOS: `isSimulatedBySoftware`、Android: `isFromMockProvider` を送信させる。違反3回で7日間の位置系機能停止 |
| 歩数偽装（振り機） | 220歩/分 超の区間無効、アクティビティ認識が `stationary` の区間は無効 |
| 端末時計操作 | すべての日付判定はサーバー時刻 |
| 写真の使い回し | 画像ハッシュ（pHash）を保存し、同一ユーザーの過去写真と 90% 以上一致なら生成拒否（枠は消費しない） |
| ステータス改ざん | ステータス・乱数はすべてサーバー生成、クライアントは表示のみ |
| API直叩き | Firebase App Check（iOS: DeviceCheck / Android: Play Integrity）必須 |

---

## 8. 画面一覧とUIフロー

### 8.1 画面一覧

| ID | 画面 | 主要素 |
|---|---|---|
| S01 | スプラッシュ／ログイン | Apple / Google サインイン、利用規約同意 |
| S02 | チュートリアル | 1枚撮影→誕生→歩行説明（3分） |
| S03 | ホーム | 今日の残り撮影枠、今日の歩数と VP、パートナー表示、お知らせ |
| S04 | カメラ | 撮影、顔検出警告、生成中アニメーション（卵） |
| S05 | 誕生演出 | 生成画像のリビール、初期ステータス表示、アイテム獲得 |
| S06 | 牧場（一覧） | アクティブ／保管の切替、ソート（レベル／合計値／誕生日／ファミリー）。※成長タイプ・素質ではソートできない |
| S07 | モンスター詳細 | ステータス、成長グラフ（実測のみ）、出自写真、技、師匠・弟子、トレーニング／アイテム使用 |
| S08 | トレーニング | 5種選択、VP残高、疲労ゲージ、結果演出 |
| S09 | マップ | 現在地、ジムピン、すれ違い履歴 |
| S10 | ジム | 防衛一覧、挑戦、配置 |
| S11 | バトル | ターン制UI、オート切替、結果 |
| S12 | 図鑑 | 発見したファミリー×属性の組み合わせ、被写体ラベル履歴 |
| S13 | アイテム | 所持一覧、使用、VP交換 |
| S14 | 師匠／継承 | 師匠任命、弟子予約、家系図表示 |
| S15 | 設定 | 通知、権限、データ削除、プレミアム管理 |

### 8.2 権限リクエストのタイミング

- カメラ: S04 初回起動時
- 位置情報（使用中のみ）: S09 初回起動時
- モーション／歩数: チュートリアル S02 の「歩く」説明直後
- 通知: 最初のモンスター誕生後（「育て時」通知の説明つき）

---

## 9. 技術構成

### 9.1 選定と理由

| レイヤ | 採用 | 理由 |
|---|---|---|
| クライアント | **Flutter 3.x（Dart）** | iOS/Android 単一コード。カメラ・歩数・位置のプラグインが揃い、Claude Code との相性が良い。UIの自由度が高い |
| 状態管理 | Riverpod 2 | テスト容易、非同期に強い |
| ローカルDB | Drift（SQLite） | オフライン時の歩数バッファ、キャッシュ |
| バックエンド | **Firebase**（Auth, Firestore, Cloud Functions 2nd gen, Storage, App Check, FCM） | 個人開発で運用負荷が最小。無料枠が広い。Functions は TypeScript |
| 画像認識 | Google Cloud Vision API（Label Detection + Image Properties + Face Detection） | ラベル・主要色・顔検出を1回のリクエストで取得できる |
| 画像生成 | OpenAI Images API（gpt-image 系）を第一候補。差し替え可能なアダプタ層を用意し Stability / Google Imagen にも切替可 | 商用利用可・日本語プロンプト可・出力サイズ指定可 |
| 地図・POI | flutter_map（OpenStreetMap）＋ Overpass API で公園・駅を取得しジム候補にする | Google Maps SDK の従量課金回避 |
| 歩数 | iOS: `pedometer` + `CMMotionActivity`、Android: Health Connect + ActivityRecognition | |
| 分析 | Firebase Analytics + Crashlytics | |
| CI | GitHub Actions（テスト）＋ Codemagic または fastlane（ストア提出） | |

### 9.2 主要 Flutter パッケージ

```
camera, image, exif, google_mlkit_face_detection（端末内顔検出）,
pedometer, health（Health Connect）, geolocator, flutter_map, latlong2,
firebase_core, firebase_auth, cloud_firestore, cloud_functions,
firebase_storage, firebase_app_check, firebase_messaging,
flutter_riverpod, drift, go_router, cached_network_image, lottie
```

### 9.3 リポジトリ構成（モノレポ）

```
snapmon/
  app/                 # Flutter
    lib/
      core/            # theme, router, di, utils
      features/
        auth/ camera/ monster/ training/ battle/ map/ gym/ items/ dex/ settings/
      data/            # repositories, dto, drift
      domain/          # entities, usecases（純Dart・テスト対象）
    test/
  functions/           # Firebase Cloud Functions (TypeScript)
    src/
      generate/        # 写真→モンスター生成パイプライン
      steps/           # 歩数検証・VP変換
      battle/          # 戦闘解決（サーバー権威）
      gym/
      shared/          # 乱数・成長式（domain と同じ式を実装、テストで一致確認）
    test/
  shared-config/       # ラベル→ファミリー辞書、技テーブル、つぶやき（murmurs.json）、定数（JSON）
  docs/                # 本書、ADR
```

成長式・ダメージ式は **サーバー側が正** とし、クライアントは表示用の予測にのみ同じ式を持つ。両者の一致をゴールデンテストで担保する。

---

## 10. データモデル

Firestore コレクション設計（主要のみ）。

### users/{userId}

```
displayName, createdAt, premiumUntil,
dailyState: { date, snapsUsed(0-3), vpEarnedToday, trainingsToday, stepsToday },
vpBalance, genes: [geneId], banFlags: { locationStrikes }
```

### monsters/{monsterId}

```
ownerId, createdAt, name,
family, subFamily?, element,
sourceLabel, sourceColors[3], sourceImagePath（Storage, ownerのみ読める）,
artImagePath, artStatus: pending|ready|failed,
level, exp,
base: {hp,atk,def,spa,spd,luk},        # 誕生時
talent: {…}（1-10）,                    # 隠し・クライアントに絶対に返さない
growth: early|avg|late|wave|superlate, # 隠し・クライアントに絶対に返さない
personality, personalityRevealed: bool,
stats: {…},                            # 現在値
statHistory: [{level, stats}],         # 成長グラフ用（実測のみ）
trainingBonus: {…},
fatigue(0-100),
bornAt,
status: active|stored,
isMentor: bool, mentorId?, discipleId?, mentorUsed: bool,
mentorMoveId?,          # 師匠として渡す技（任命時に確定）
inheritedMove?: { moveId, generation },  # 弟子として受け取った技（5枠目）
murmurRate,             # 0.5-1.5（隠し）
moves: [moveId x4],
phash
```

**セキュリティ規則**: Firestore ルールでクライアントからの `monsters` 直接読み取りを禁止し、必ず Cloud Functions 経由で `talent` `growth` を除いた DTO を返す。もしくは `monsters_private/{monsterId}` に隠し値だけ分離し、クライアントのルールを deny にする（推奨）。

### steps/{userId}/segments/{segmentId}

```
startAt, endAt, steps, activity: walking|running|cycling|automotive|stationary|unknown,
avgSpeedKmh, isMock, accepted: bool, rejectReason?
```

### gyms/{gymId}

```
name, lat, lng, osmId, defenders: [{monsterId, ownerId, placedAt}], controllingTeam?
```

### battles/{battleId}

```
type: gym|passby|friend|league, attackerId, defenderId,
attackerParty[3], defenderParty[3], seed, log[], result, createdAt
```

### items: users/{userId}/inventory/{itemId} → `{ type, count }`

### 静的マスタ（shared-config、Functions と app 両方にバンドル）

- `label_map.json`: Vision ラベル → family
- `moves.json`: 技ID, family/element, power, category, effect
- `constants.json`: 上限値、レート、成長曲線係数

---

## 11. サーバーAPI設計（Cloud Functions, callable）

すべて認証＋App Check 必須。レスポンスは `{ ok, data | error }`。

| 関数 | 入力 | 処理 | 出力 |
|---|---|---|---|
| `generateMonster` | `imageBase64`（≤1MB） | §3.1 のパイプライン。枠チェック→Vision→顔・pHash 検査→乱数→Monster作成→画像生成ジョブ投入 | `monsterId, family, element, base, items[]` |
| `onArtGenerated`（内部・非同期） | ジョブ | 画像生成API呼出→Storage保存→`artStatus=ready`→FCM通知 | — |
| `submitSteps` | `segments[]` | §5.2 判定→受理分を VP に変換→`dailyState` 更新 | `acceptedSteps, vpGained, vpBalance` |
| `train` | `monsterId, type` | VP・回数・疲労チェック→上昇計算→保存 | `statsDelta, fatigue` |
| `useItem` | `monsterId, itemId` | 効果適用 | 更新後の monster |
| `startBattle` | `type, targetId, party[]` | シード生成→サーバーで全ターン解決→ログ保存 | `battleId, log, result, rewards` |
| `placeDefender` / `recallDefender` | `gymId, monsterId` | 距離検証（80m）→配置 | — |
| `appointMentor` | `monsterId, mentorMoveId` | Lv50 検証、技所持検証→`isMentor=true` | — |
| `getMurmur` | `monsterId, trigger` | §4.6 の重み付き抽選。ログは残さない | `murmurTextId` |
| `reserveDisciple` | `mentorId, useCapsule` | `mentorUsed` 検証→次回 `generateMonster` に継承を予約 | — |
| `setStorage` | `monsterId, stored: bool` | アクティブ／保管の移動、枠検証 | — |
| `getNearbyGyms` | `lat, lng` | 半径 2km のジム＋未登録POIならOverpassから生成 | gyms[] |
| `deleteAccount` | — | GDPR/APPI 対応の完全削除 | — |

レート制限: `generateMonster` は 1分1回、`submitSteps` は 5分1回、`startBattle` は 10秒1回。

---

## 12. マネタイズ

原則: **強さと撮影枠は売らない。**

| 商品 | 価格目安 | 内容 |
|---|---|---|
| プレミアム（月額） | 480円 | 所持上限100体、出自写真のクラウドバックアップ、広告なし、成長グラフの詳細表示、着せ替え枠 |
| 着せ替え・背景 | 120〜360円 | 見た目のみ |
| 高解像度アート | 240円/体 | 生成画像の 2048px 版と壁紙 |
| 名前変更・牧場拡張（見た目） | 120円 | |

広告: 無料ユーザーの「誕生演出後」に 1日1回まで、リワード型（VP +5）を任意視聴。

---

## 13. 開発フェーズとマイルストーン

| フェーズ | 期間目安 | ゴール | 完了条件 |
|---|---|---|---|
| **P0 基盤** | 1〜2週 | リポジトリ、Firebase、認証、CI | サインインしてホームが出る |
| **P1 誕生** | 2週 | カメラ→Vision→乱数→仮画像（プレースホルダ）→詳細画面 | 自分のスマホで撮影して個体が生まれる。ステータス生成のユニットテスト完備 |
| **P2 育成** | 2週 | 歩数同期、VP、トレーニング、成長式、成長グラフ | 1日歩いて育つ。乗り物除外が動く |
| **P3 アート** | 1週 | 画像生成API接続、非同期リビール、コスト監視 | 生成画像が表示される。失敗時のフォールバック |
| **P4 バトル** | 2〜3週 | 技、ダメージ式、フレンド非同期対戦、オート | サーバー解決のバトルが再現可能（同シード同結果） |
| **P5 位置** | 2〜3週 | マップ、ジム、POI取込、位置偽装対策 | 近所の公園に防衛を置いて他端末から挑戦できる |
| **P6 α** | 2週 | 図鑑、アイテム、師匠・継承・家系図、保管牧場、チュートリアル | 家族・友人 5〜10人で TestFlight / 内部テスト |
| **P7 β** | 3〜4週 | バランス調整、課金、広告、利用規約・プライバシーポリシー | 100人規模オープンβ |
| **P8 リリース** | 2週 | ストア申請、審査対応 | 公開 |

合計 4〜5ヶ月（週10〜15時間の副業ペース想定）。Claude Code がコードの大半を書く前提で、人間の作業は動作確認・実機テスト・アカウント設定・審査提出が中心。

---

## 14. コスト見積もりとリスク・法務

### 14.1 固定費

| 項目 | 費用 |
|---|---|
| Apple Developer Program | 年 99 USD |
| Google Play 開発者登録 | 初回 25 USD |
| Firebase | Sparkプラン（無料）で β まで。Blaze 移行後も 1,000 DAU で月 1〜3千円程度 |
| ドメイン・LP | 年 2千円前後 |

### 14.2 変動費（1ユーザー1日3枚生成の場合）

| 項目 | 単価目安 | 1,000 DAU/月 |
|---|---|---|
| Vision API（3機能） | 約 0.5円/枚 | 約 4.5万円 |
| 画像生成（1024px 標準品質） | 約 3〜6円/枚 | 約 27〜54万円 |

画像生成が最大コストになる。対策として (a) 実際の生成率は DAU の 40〜60% と見込む、(b) 無料ユーザーは 512px 生成・プレミアムは 1024px、(c) ファミリー×属性ごとにベース画像を事前生成し、写真ごとの差分は色変換・パーツ合成で済ませる「ハイブリッド方式」を P3 で比較検証する。**P3 の完了条件に「1枚あたり生成コストの実測」を含める。**

### 14.3 リスクと対応

| リスク | 対応 |
|---|---|
| 写真に人・他人の物・個人情報が写る | 端末内で顔検出→拒否。出自写真は本人の端末／本人のみ読めるStorageに限定。他ユーザーには生成アートのみ公開 |
| 生成画像が既存キャラクターに似る | プロンプトに「既存キャラクター名・作品名を含めない」、ネガティブプロンプト、出力の類似判定は β で人手レビュー |
| 画像生成APIの規約変更・値上げ | アダプタ層で差し替え可能にする（§9.1） |
| 位置情報ゲームの安全性（歩きスマホ・私有地） | 移動中はバトルUIをロック（速度 3km/h 超で警告）、ジムは公共POIのみ |
| 健康配慮 | 1日 12,000歩で VP 上限。それ以上歩いても「無駄」にする |
| 未成年 | 13歳未満はサインイン不可（Apple/Google の年齢情報）。位置共有機能はデフォルトオフ |
| 法務 | 利用規約、プライバシーポリシー（個人情報保護法・APPI、位置情報・生体情報の扱い）、特定商取引法表記（課金）、資金決済法（ゲーム内通貨 VP は無償付与のみで課金購入不可にして対象外に） |
| ストア審査 | 「ユーザー生成コンテンツ」要件（通報・ブロック機能）が必要。フレンド機能に通報ボタンを実装 |

---

## 15. 実装の進め方（Claude Code への指示）

以下は VSCode 上の Claude Code に対する作業指示である。

### 15.1 最初にやること

1. §9.3 のリポジトリ構成で `snapmon/` を作成する。Flutter は `flutter create --org jp.shipi app`（org は変更可）。
2. `shared-config/constants.json` に §4, §5, §6, §7 の定数をすべて書き出す。**マジックナンバーをコードに直書きしない。**
3. `app/lib/domain/growth.dart` と `functions/src/shared/growth.ts` に §4.3・§4.4 の式を実装し、同一入力で同一出力になるゴールデンテスト（JSON フィクスチャ共有）を先に書く。
4. `docs/ADR-001-stack.md` に §9.1 を写し、変更したら ADR を追加する。

### 15.2 フェーズ順守

§13 の P0→P8 の順で進める。各フェーズは「完了条件」を満たすまで次に進まない。画像生成 API（P3）はコストが絡むため、P1〜P2 はプレースホルダ画像（ファミリー×属性の SVG シルエット 84 種）で進める。

### 15.3 コーディング規約

- Dart: `flutter_lints` 準拠、`domain/` は Flutter 非依存の純 Dart にしてテスト可能に。
- TypeScript: `strict: true`、`zod` で callable の入力を検証。
- 乱数はすべて `seed` 付きで再現可能にする（`seedrandom` / Dart は自前 xorshift）。
- すべての callable に 1 つ以上のユニットテストとエミュレータ統合テスト。
- コミットは 1 機能 1 コミット、日本語 OK。

### 15.4 人間（しぴ）に依頼が必要な作業

Claude Code は以下に当たったら止まって依頼すること。

- Firebase プロジェクト作成と `google-services.json` / `GoogleService-Info.plist` の配置
- Apple Developer / Google Play アカウント、署名証明書
- Cloud Vision / 画像生成 API のキー発行と課金上限設定（**必ず月額上限を設定**）
- 実機での動作確認（カメラ・歩数・位置は実機でしか検証できない）
- 利用規約・プライバシーポリシーの最終確認

### 15.5 未決事項（実装しながら決める）

- 正式タイトル（仮: SnapMon）
- 技の名称・演出（moves.json の日本語名）
- ジムの「チーム」概念を入れるか（ポケモンGO 型 3 陣営）。P5 開始時に判断
- ハイブリッド画像方式の採否（P3 のコスト実測後）
- つぶやきシステム（§4.6）の出現率・重みの最終値。βで「超晩成個体を Lv20 前後で保管送りにする率」を計測して調整

---

## 16. 付録

### 16.1 乱数・シード

```
seed = sha256( sourceLabel + "|" + dominantColorHex + "|" + userId + "|" + yyyymmdd + "|" + serverNonce )
rng  = xorshift128(seed の先頭 16 バイト)
```

serverNonce により同一条件でも予測不能。ただし種族・属性はラベルと色から決定論的。

### 16.2 定数表（constants.json 初期値）

```json
{
  "snapsPerDay": 3,
  "stepsPerVp": 100,
  "maxStepsPerDay": 12000,
  "maxVpBalance": 200,
  "trainingCostVp": 10,
  "trainingsPerDay": 8,
  "maxSpeedKmhForSteps": 12,
  "maxStepsPerMinute": 220,
  "gymRadiusM": 80,
  "gymChallengesPerDay": 3,
  "defenderAutoReturnHours": 24,
  "activeCapFree": 30,
  "activeCapPremium": 100,
  "storageCap": 500,
  "levelCap": 50,
  "statCapBase": 399,
  "statCapPerTalent": 60,
  "baseStatRange": [5, 60],
  "talentRange": [1, 10],
  "mentorInheritRate": 0.3,
  "mentorInheritRateCapsule": 0.5,
  "fatigueCurePerDay": 1,
  "murmurHintRateInWindow": 0.08,
  "murmurHintRateOutOfWindow": 0.01,
  "murmurWindowJitterLevels": 4,
  "murmurRateRange": [0.5, 1.5],
  "legacyMoveGenerations": 3,
  "legacyMovePowerBonus": 0.10,
  "phashRejectSimilarity": 0.9,
  "faceDetectReject": true
}
```

### 16.3 画像生成プロンプトテンプレート

```
A single original fantasy creature for a mobile monster-raising game.
Family: {family} ({familyDescription}). Element: {element}.
Inspired by the shape and texture of: {sourceLabel}.
Primary colors: {color1}, {color2}, {color3}.
Style: clean cel-shaded illustration, soft outline, full body, centered,
facing slightly left, plain white background, no text, no watermark.
Personality vibe: {personality}.
Do not depict any existing copyrighted character, brand, or logo.
Do not depict humans or human faces.
```

ネガティブ: `text, watermark, logo, human, realistic photo, multiple creatures, existing franchise character`

### 16.4 成長曲線の検算（各タイプ Lv1→50 の curve 合計）

| タイプ | 合計 |
|---|---|
| 早熟 | 61.75 |
| 平均 | 62.5 |
| 晩成 | 63.25 |
| 波型 | 62.48 |
| 超晩成 | 29×0.3 + 21×2.56 = 62.46 |

全タイプが 62.5 ± 1 に収まることを確認済み。定数を変更する場合は `test/growth_curve_test` の合計値アサーション（62.5 ± 2）を通すこと。

---

以上。
